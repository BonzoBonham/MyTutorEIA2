<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nicolasperez.mytutoreia">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PaginaPerfil"
            android:label="@string/title_activity_pagina_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistroAsesor"
            android:label="@string/title_activity_pagina_registro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistroGeneral"
            android:label="@string/title_activity_registro_general"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistroEstudiante"
            android:label="@string/title_activity_registro_estudiante"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>